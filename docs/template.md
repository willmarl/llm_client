## This func does ...

> `foo(bar)`

```python
print("hi")
```

**Description**:
Initialize LLM for langchain. Gets the LLM based on provider and model from config.

**Parameters**:
params

**Returns**:
returns
