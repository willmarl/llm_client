langchain==1.2.10
langchain-openai==1.1.10
langchain-anthropic==1.3.3
langchain-ollama==1.0.1
langchain-text-splitters
langgraph
python-dotenv
pydantic
rich==14.3.2

# Optional installs:
#   pip install -e '.[ocr]'
#   pip install -e '.[rag]'
#   pip install -e '.[vision]'
#   pip install -e '.[full]'
#
# Local CLIP still needs manual torch/CLIP setup. For AMD/ROCm, install torch
# from the PyTorch ROCm index before using local image embeddings.