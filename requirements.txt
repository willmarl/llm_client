langchain==1.2.10
langchain-chroma==1.1.0
langchain-openai==1.1.10
langchain-anthropic==1.3.3
langchain-ollama==1.0.1
langchain-community==0.4.1
rich==14.3.2
pillow==12.1.1
replicate==1.0.7
unstructured==0.21.5
pdfminer-six==20260107
pi-heif==1.3.0
unstructured-inference==1.5.2
pdf2image==1.17.0
 pytesseract==0.3.13
 unstructured-pytesseract==0.3.15
torch --index-url https://download.pytorch.org/whl/rocm6.2
git+https://github.com/openai/CLIP.git
torch==2.5.1+rocm6.2
torchvision==0.20.1+rocm6.2
questionary==2.1.1

# because i have AMD gpu, need rocm
# need to install `torch --index-url https://download.pytorch.org/whl/rocm6.2` manually.
# cant do `uv pip install -r requirements.txt` will always fail
# so for setup after activating venv do theses commands first before doing `install -r requirements.txt`
# `uv pip install torch --index-url https://download.pytorch.org/whl/rocm6.2`
# `uv pip install torchvision`