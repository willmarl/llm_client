The Chroma vector database stores document embeddings for semantic search and retrieval. It's built on top of LangChain and Hugging Face embeddings. Chroma handles the full pipeline: embedding generation, storage, and similarity search. You can persist Chroma databases to disk for long-term storage. Chroma supports filtering, metadata attachment, and efficient similarity queries on millions of documents.
